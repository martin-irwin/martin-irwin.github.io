{
	"name": "Existing Dockerfile",
	"build": {
		"context": "..",
		"dockerfile": "../Dockerfile"
	},
	"postCreateCommand": "git config --global user.email 'martin@kuroshi.net' && git config --global user.name 'Martin Irwin'"
}